# =============================================================
# FlowPay SDK — Variáveis de Ambiente / Environment Variables
# Copie este arquivo para `.env` e preencha os valores.
# Copy this file to `.env` and fill in the values.
# NUNCA versione o arquivo `.env` real. / NEVER commit your real `.env`.
# =============================================================

# Chave de API da FlowPay (fp_test_xxx para testes, fp_live_xxx para produção)
# FlowPay API key (fp_test_xxx for sandbox, fp_live_xxx for production)
# Obtenha em / Get yours at: https://flowpayments.net/dashboard/api
FLOWPAY_API_KEY=
